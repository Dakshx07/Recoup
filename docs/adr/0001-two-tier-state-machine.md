# ADR 0001: Two-Tier State Machine

> Status: **Pending** — to be written at build-order step 5.

## Context

_Why separate `recovery_cases` and `commitments` state machines instead of one flat machine._

## Decision

_Pending._

## Consequences

_Pending._
